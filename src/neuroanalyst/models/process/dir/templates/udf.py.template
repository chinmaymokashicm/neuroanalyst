"""
udf.py - User-Defined Function for ${process_id} - ${process_name}

This file contains the main processing function that implements the core logic
of the NeuProcess. The function defined here will be wrapped by the NeuProcess
framework in wrapper.py.

Version: ${version}
Created: ${creation_date}
Author: ${author}
"""

from typing import Dict, Any, Union
from pathlib import Path

# Define the function without decoration
${import_statements}

${udf_code}
